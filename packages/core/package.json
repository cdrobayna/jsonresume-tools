{
  "name": "@jsonresume-tools/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared internal utilities for jsonresume-tools packages: types, severity routing, reporters, config loading, and a CLI harness.",
  "engines": {
    "node": ">=22"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "cosmiconfig": "^9.0.0"
  }
}
